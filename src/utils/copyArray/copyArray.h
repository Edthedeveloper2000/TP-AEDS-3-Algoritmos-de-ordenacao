#include "../../types/Card/card.h"

/**
 * Cria uma copia do array passado
 * @param source - array de origem
 * @param size - tamanho do array
 * @return Card* - retorna um novo vetor identico ao primeiro
*/
Card* copyArray(const Card* source, int size);