#include "../types/Card/card.h"

#ifndef FSORT_H
#define FSORT_H

/**
 * Realiza a ordenação de um array de cartas independentemente
 * para cada algoritmo de ordenação e exibe as informações
 * de tempo, movimentos e comparação no arquivo de saida.
 * @param cards - array a ser ordenado
 * @param testNumber - numero do teste a ser realizado
*/
void fsort(Card cards[], int testNumber);


#endif // FSORT_H
